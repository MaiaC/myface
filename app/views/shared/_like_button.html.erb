<% if post.likes.find_by(user_id: current_user.id) %>
  <%= link_to "Unlike", like_path(post), method: :delete %>
<% else %>
  <%= link_to "Like", likes_path(id: post.id, type: "Post"), method: :post %>
<% end %>